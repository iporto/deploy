# Vhost de produção (Coolify termina o TLS na borda; aqui é só HTTP interno na 80).
server {
    listen 80 default_server;
    server_name _;
    root /var/www/html/public;
    index index.php;

    charset utf-8;
    client_max_body_size 50M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # php-fpm da imagem base escuta em 127.0.0.1:9000 (mesmo container, via supervisor).
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    # Nega dotfiles (exceto .well-known, usado por ACME/Apple/etc.).
    location ~ /\.(?!well-known).* {
        deny all;
    }
}
